
services:
  - type: web
    name: cds-flashcard-base
    env: static
    buildCommand: npm install
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
